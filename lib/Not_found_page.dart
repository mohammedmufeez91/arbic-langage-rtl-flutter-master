import 'package:flutter/material.dart';
class Not_found extends StatefulWidget {
  @override
  _Not_foundState createState() => _Not_foundState();
}

class _Not_foundState extends State<Not_found> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Not found'),),
    );
  }
}
